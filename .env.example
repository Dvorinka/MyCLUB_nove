# MyClub – example environment configuration
#
# Copy this file to `.env.local` (for local development) and fill in
# the values with your own secrets. Never commit real secrets to git.

# Node / Next.js
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# GoCardless API (payments) – use sandbox for development
# Visit https://manage.gocardless.com/ to create a sandbox account and credentials.
GOCARDLESS_API_KEY=gc_sandbox_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GOCARDLESS_ENVIRONMENT=live   # or "live"

# Webhook secret used to verify GoCardless webhooks
# Configure this in the GoCardless dashboard and keep it secret.
GOCARDLESS_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: application-level config for pricing/checkout
# Used to build return URLs and links in the app.
PAYMENTS_SUCCESS_URL=http://localhost:3000/cenik?status=success
PAYMENTS_CANCEL_URL=http://localhost:3000/cenik?status=cancelled
